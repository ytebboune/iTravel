export enum SortField {
  CREATED_AT = 'createdAt',
  UPDATED_AT = 'updatedAt',
  NAME = 'name',
  PRICE = 'price',
  RATING = 'rating',
  SCORE = 'score',
  DURATION = 'duration',
  DATE = 'date'
}

export enum SortOrder {
  ASC = 'asc',
  DESC = 'desc'
} 